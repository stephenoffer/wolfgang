# Generated output scores (regenerable) and per-piece working state.
# NOTE: corpus data under tools/ (compiled_packs, reference_index,
# pattern_library, texture_templates, context_*) IS tracked — do not ignore it.
output/
workspace/

# Raw reference scores — large (200MB+), regenerable third-party clones with
# their own nested .git repos. Bar records / compiled corpus artifacts are
# tracked instead; raw scores are not needed in this repo.
tools/reference_scores/

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
